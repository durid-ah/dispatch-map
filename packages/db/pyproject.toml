[project]
name = "db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Durid Ahmad", email = "durid.ahmad.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "sqlmodel>=0.0.39",
]

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"
