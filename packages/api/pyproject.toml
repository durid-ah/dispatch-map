[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "db",
    "fastapi[standard]>=0.139.0",
]

[tool.uv.sources]
db = { workspace = true }
